node_modules/
dist/
.vite/
*.tsbuildinfo

# Payloads under public/*.json ARE committed: they're regenerated by
# `scripts/build_demo_payload.py` and shipped to GitHub Pages as part of
# the static site. ~37 MiB total today. If repo size becomes a problem,
# either: (a) move to Git LFS, or (b) generate them inside the
# deploy-pages workflow (the skeleton cache under tests/fixtures/ is
# already enough; no neuPrint token needed).
